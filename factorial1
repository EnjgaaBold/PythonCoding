# Python code​​​​​​‌​‌‌‌‌‌​‌‌​‌​​‌‌​‌‌‌​‌​​​ for finding factorial value of num

def factorial(num):
    # Your code goes here.
    if type(num) == int and num >= 0:
        if num >= 0:
            factorial = 1
            count = num
            while count > 0:
                factorial = factorial * count
                count = count - 1
            return factorial
        else:
            return 1
    else:
        return None
